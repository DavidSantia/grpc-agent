// Wrapper for New Relic Go Agent

syntax = "proto3";

option go_package = ".;protos";

package protos;

service CreateApp {
  rpc CreateApp (AppConfig) returns (AppReply) {};
}

message AppConfig {
  string name = 1;
  string license = 2;
}

message AppReply {
  string message = 1;
}
